# Лог наблюдений

<!--
Формат: заголовки по датам с наблюдениями по emoji-приоритетам.
🔴 Высокий: ключевые факты, решения, выводы, важные источники
🟡 Средний: гипотезы, полезный контекст, промежуточные находки
🟢 Низкий: эфемерные заметки, поисковые запросы, мелкие детали
Временные метки в формате ЧЧ:ММ. Включай временные якоря где уместно.
Тройной временной якорь: (дата наблюдения, упомянутая дата, относительная дата)
-->

## Дата: 2026-04-21

- 🔴 19:26 Пользователь просит security-анализ и описание fork'а `msdanyg/smart-connections-mcp`. Fork чистый, правок нет (git log: один коммит "Initial release v1.0.0").
- 🔴 19:28 Изучил все 5 файлов в `src/` (index.ts, smart-connections-loader.ts, search-engine.ts, embedding-utils.ts, types.ts). Сервер — read-only loader embeddings из `<vault>/.smart-env/` + 6 MCP tools.
- 🔴 19:29 Security-скан кода: НЕТ `fetch`/`http`/`net`/`spawn`/`exec`/`child_process`/`eval`/`Function(`. Только `fs.readFileSync`, `readdirSync`, `JSON.parse`, вычисления над векторами. stdio через MCP SDK.
- 🔴 19:30 **Находка (Medium-severity)**: path traversal в [smart-connections-loader.ts:141-147](src/smart-connections-loader.ts#L141-L147) — `path.join(vaultPath, notePath)` без containment check. LLM может передать `../../.ssh/id_rsa` через tool `get_note_content`, сервер вернёт содержимое. Вектор эксплуатации — prompt injection через заметки.
- 🔴 19:31 **Находка**: `search_notes` реально делает substring-regex по файлам ([search-engine.ts:177-218](src/search-engine.ts#L177-L218)), не семантический поиск. Описание tool в index.ts вводит LLM в заблуждение.
- 🔴 19:32 Зависимости легитимны: `@modelcontextprotocol/sdk@1.29.0` + `zod@3`. Нет малоизвестных пакетов.
- 🔴 19:33 `dist/smart-connections-loader.js` — чистая tsc-компиляция `src/smart-connections-loader.ts`, никаких посторонних добавлений (проверил вручную). Рекомендация пользователю: всё равно пересобрать `npm run build`.
- 🟡 19:34 Прочитал описание с plugable.io: разработчик заявляет "all computation on your machine, no external calls" — подтверждается кодом. НО в описании указано "3 tools", а в коде реально 6 (get_similar_notes, get_connection_graph, search_notes, get_embedding_neighbors, get_note_content, get_stats). plugable.io описывает старую версию.
- 🟡 19:35 Обновил `CLAUDE.md`: добавил разделы Project, Commands, Architecture (4-слойный pipeline), Non-obvious behavior (AJSON-парсинг, model key resolution, keyword-only search_notes, плоский граф).
- 🟢 19:36 Fork remote: `origin = ushakov-git/smart-connections-mcp`, `upstream = msdanyg/smart-connections-mcp`. `git diff upstream/main HEAD` не сработал — upstream не зафетчен локально. Не критично.
- 🟢 19:37 AJSON-формат `.smart-env/multi/*.ajson` — нестандартный JSON: каждая строка `"key": {...},` с trailing comma. Loader strip + wrap `{}` перед `JSON.parse`. Хрупко к изменениям в Smart Connections.
- 🟢 19:38 `getConnectionGraph` возвращает плоский список, а не дерево (тип `ConnectionNode` остался от предыдущей версии).
- 🔴 19:50 **Критично для плана доработок**: пользовательский vault `/Users/evgeny/Dropbox/8 SecondBrain/OBSIDIAN-NOTION/Develop/Develop/` использует Ollama+bge-m3 (1.2GB, мультиязычная, русский). В `.smart-env/smart_env.json` поле `embedding_models.default_model_key = "ollama#1776670752600"`, но **legacy-поле** `smart_sources.embed_model` всё ещё указывает на устаревший `transformers/TaylorAI/bge-micro-v2`. Реальный ключ в `multi/*.ajson` — `embeddings["bge-m3:latest"]`. Текущий MCP-сервер не заработает вообще.
- 🔴 19:51 **Реальная размерность bge-m3 = 1024** (замерено грепом `vec` в первом ajson). В метаданных `embedding_models.ajson` ошибочно указано `dims:384` — Smart Connections баг. Вывод: **autodetect dims рантайм**, не доверять метадате.
- 🔴 19:52 Новый актуальный формат Smart Connections: отдельный `embedding_models/embedding_models.ajson` с записями вида `"embedding_models:<provider>#<ts>": {provider_key, model_key, dims, host:"http://localhost:11434", endpoint:"/api/embed", ...}`. Это даёт сервер endpoint для семантического поиска query.
- 🔴 19:53 `multi/*.ajson` содержит параллельно `smart_sources:` И `smart_blocks:<path>#<heading>` с собственными эмбеддингами. Текущий loader выбрасывает блоки. Для block-level поиска (запрос пользователя #4 — «ссылка на файл и раздел») нужно их поднимать.
- 🔴 19:58 Составлен и выдан пользователю план v2.0.0 на 7 фаз: (0) подготовка; (1) security fixes; (2) поддержка bge-m3 и авто-детект формата; (3) block-level индекс + obsidian_uri + excerpt + новый tool `get_block_content`; (4) настоящий семантический `search_notes` через Ollama `/api/embed` на localhost:11434; (5) hot-reload watcher + graceful shutdown; (6) эргономика MCP (meta в ответах, `resolve_link`); (7) docs.
- 🟡 19:59 Четыре открытых вопроса к пользователю перед стартом: bundle vs series PR; сохранять ли legacy TaylorAI fallback; обязательность Ollama (semantic opt-in?); формат `obsidian_uri` (требуется ли `obsidian://` с именем vault'а).
- 🟡 20:00 Пользователь ответил только путём к vault: `/Users/evgeny/Dropbox/8 SecondBrain/OBSIDIAN-NOTION/Develop/Develop/` (корень, не `.smart-env`). Имя vault для `obsidian://` = `Develop`. Остальные 3 вопроса — без ответа.
- 🔴 20:15 Пользователь ответил на 4 исходных вопроса: (1) серия PR, но pushing в публичный upstream НЕ планируется — только origin; (2) legacy TaylorAI НЕ нужен, выпиливаем; (3) Ollama opt-in; (4) `obsidian://` URI не нужен — годится относительный путь, но каждый vault получает своё имя через env (user будет регистрировать отдельный MCP-инстанс на каждый vault).
- 🔴 20:18 Создал ветку `feat/v2-bge-m3-and-hardening`. Добавлены: `src/env-loader.ts` (zero-dep .env, MCP-client config wins), `src/ajson-parser.ts` (вынесенный parseAjsonLines), `src/embedding-models-loader.ts` (читает embedding_models/embedding_models.ajson, индексирует byFullKey + byModelKey).
- 🔴 20:22 Переписан `src/smart-connections-loader.ts`: новый `resolveActiveModel()` (env → default_model_key → autodetect по частоте в sources; legacy удалён полностью). Runtime-detect dims. Фильтрация sources без вектора под активной моделью. Path-traversal guard в readNoteContent/extractBlockContent: rejects абсолютные, `..`, symlink-escape (через realpath). Статистика kept/replaced/skipped/parseErrors + startup-лог.
- 🔴 20:25 Smoke-test `test-bge-m3.mjs` на реальном vault пользователя — 11/11 PASS: модель=`bge-m3:latest` через `default-model-key`, dims=1024, 140 sources kept, 8 replaced (заметки-призраки после перемещений в Smart Connections), 3 без эмбеддинга, traversal-защита блокирует `../../../etc/passwd` и `/etc/passwd`. Top-similarity 0.879 между MASTRA-заметкой и её NotebookLM-копией.
- 🔴 20:30 Коммит `4a85474` в ветке (23 файла, +1242/−324). НЕ запушен. Пользователь сам решит когда пушить в origin `ushakov-git/smart-connections-mcp`.
- 🟡 20:32 Метадата `embedding_models.ajson` для bge-m3 пишет `dims:384` — это баг Smart Connections. Реально вектора 1024. Значит doверять метадате нельзя — детект рантайм обязателен для любой не-дефолтной модели.
- 🟢 20:35 package-lock.json был модифицирован ДО сессии (видно в git status на старте), не трогаю без явного npm install.
- 🟢 20:36 Пользователь дал команду «Продолжай Фазу 3». Фаза 3: smart_blocks index, ссылки на раздел (path+heading+lines+excerpt+vault_name), новый tool get_block_content, nested граф связей.
- 🔴 20:50 Фаза 3 закомичена (`4ca863b`, 16 файлов, +1494/−839). На vault пользователя — 11 686 блоков kept, 9357 без эмбеддинга (короче min_chars=200), 12 replaced. Top-block-similarity 0.861, hit теперь с `heading`, `lines`, `excerpt`, `vault_name`. `getConnectionGraph` вернул плоский список+nested tree. Новые tools: `search_blocks`, `get_block_content`. 34/34 smoke-тестов.
- 🔴 21:05 Разведка Ollama: `http://localhost:11434/api/tags` показывает только `bge-m3:latest`, endpoint `/api/embed` с `{model,input}` возвращает `{embeddings:[[...1024]]}`. Dims=1024 совпадают с vault — cosine между query-vec и vault-vec валиден.
- 🔴 21:15 Написан `src/ollama-client.ts`: Node fetch + AbortController timeout 10s + LRU кэш (default 100) + dims-guard (отказ работать при mismatch во избежание порчи cosine) + non-throwing `health()` с разделением reachable/modelAvailable/dimsMatch.
- 🔴 21:20 `SearchEngine.searchByQuery` переделан в async с options-объектом и режимами `semantic`/`keyword`/`hybrid`. Hybrid = RRF с k=60, ID = `path#heading` или `path` в зависимости от гранулярности. Graceful fallback на keyword при ошибке Ollama с warning в meta.
- 🔴 21:25 `index.ts`: startup-probe Ollama → активируется только при reachable+modelAvailable+dimsMatch. Env: `OLLAMA_HOST` (fallback на `embedding_models.ajson.host`), `OLLAMA_EMBED_MODEL`, `DISABLE_SEMANTIC_SEARCH=1`. `search_notes` описание переписано честно про три режима. `meta` ответов: `semantic_available`, `search_mode`, `fallback_from`, `warnings`.
- 🔴 21:30 Smoke-тест: 43/43 зелёных. Semantic-запрос «observability и трейсинг агентов Mastra» (русский) → top-hit Mastra deep-research, sim=0.742, block-level. Hybrid работает. LRU кэш: 88ms cold → 0ms warm. Коммит `3a8a54b` (11 файлов, +760/−26).
- 🔴 21:31 Фаза 4 закомичена. Ветка `feat/v2-bge-m3-and-hardening` содержит 3 коммита: `4a85474` (Фаза 2), `4ca863b` (Фаза 3), `3a8a54b` (Фаза 4).
- 🟡 21:32 Пользователь просит закрыть всё остальное за один проход: Фаза 1-остаток (.md-whitelist, убрать dist/), Фаза 5 (watcher+shutdown), Фаза 6 (meta+resolve_link tool), Фаза 7 (docs).
- 🔴 21:50 Фаза 1-остаток закомичена (`9fa406c`): .md/.markdown/.canvas whitelist в `readNoteContent` (containment-check ПЕРЕД whitelist для лучших сообщений), `git rm --cached -r dist`, `.gitignore` обновлён. 45/45 тестов.
- 🔴 22:05 Фаза 5 закомичена (`bde3dfa`): `src/vault-watcher.ts` с `fs.watch` (multi/ + embedding_models/ + root), debounce 500ms, incremental `reloadMultiFile` + full `fullReload`. Graceful shutdown на SIGINT/SIGTERM. `DISABLE_WATCHER=1` выключает. 50/50.
- 🔴 22:20 Фаза 6 закомичена (`bcb37b4`): `src/link-resolver.ts` с поддержкой `[[Note#Heading]]`, `[[Folder/Note]]`, `[[Note|Alias]]`, `obsidian://open?vault=X&file=Y`, advanced-uri. Basename-индекс для bare-wikilinks. Warning на vault mismatch. `execution_ms`, `total_notes`, `total_blocks` в `baseMeta`. 60/60.
- 🔴 22:35 Фаза 7 закомичена (`fcc5d29`): README.md v2 полностью переписан, TROUBLESHOOTING.md с startup-log-driven секциями, CHANGELOG.md с breaking changes, `package.json` → 2.0.0 + `npm run smoke`. Итого 7 коммитов в ветке, v2.0.0 готов.
- 🔴 22:36 Финальный состав tools (9): `get_similar_notes`, `search_blocks`, `search_notes` (semantic/keyword/hybrid), `get_embedding_neighbors`, `get_connection_graph` (flat+tree), `get_note_content`, `get_block_content`, `resolve_link`, `get_stats`. Каждый ответ — `{meta:{vault_name,model_key,dims,semantic_available,total_notes,total_blocks,execution_ms,...}, ...payload}`.
- 🟡 22:40 Пользователь попросил README_rus.md — подробная русская документация для пользователя: разделы по каждому tool, работа с vault, взаимодействие с Ollama, подключение к клиентам.
- 🔴 22:55 Написан README_rus.md (1301 строка, 17 разделов). Не закоммичен. Структура: зачем → архитектура → формат `.smart-env/` → notes+blocks → резолвер модели → Ollama → три режима поиска → watcher → безопасность → установка → env → подключение клиентов (Desktop/Code CLI/Code .mcp.json/любой MCP) → мульти-vault → 9 tools детально → формат ответов → диагностика → ограничения.

## Дата: 2026-04-22

- 🔴 00:15 Пользователь просит сменить `k` в RRF на 75 с гипотезой «это усилит семантику». Это НЕВЕРНО: `k` — сглаживающий параметр, применяется одинаково к обоим спискам. Больший `k` → разница между рангами меньше (всё равно для обоих списков), но баланс semantic↔keyword НЕ смещается. Для реального смещения нужен weighted RRF: `score = w_sem * rrf_sem + w_kw * rrf_kw`.
- 🔴 00:18 Решение: реализовать weighted RRF с env-переменными `RRF_K` (default 60), `RRF_SEMANTIC_WEIGHT` (default 0.7), `RRF_KEYWORD_WEIGHT` (default 0.3). Это реально сместит hybrid к семантике. Параметр `k` настраиваемый — пусть пользователь ставит 75, если хочет, но объяснить что реальный эффект даёт weighted.
- 🟡 00:20 Пользователь просит `.mcp.json` в vault `/Users/evgeny/Dropbox/8 SecondBrain/OBSIDIAN-NOTION/Develop/Develop` с путём к коду `/Users/evgeny/Dropbox/4-Base-of-Skills/9-Instrumental-skills/Obsidian/mcp-to-obsidian-smart-connection/smart-connections-mcp/dist/index.js`. Шаблон для копирования в другие vault'ы.
- 🔴 00:40 Реализован weighted RRF: `SearchEngine` получил `FusionOptions` (k / semantic_weight / keyword_weight), формула `score = w_sem·rrf_sem + w_kw·rrf_kw`, дефолты `(60, 0.7, 0.3)`. Env-переменные: `RRF_K`, `RRF_SEMANTIC_WEIGHT`, `RRF_KEYWORD_WEIGHT` с валидацией через `parseNum`. `meta.fusion` в каждом ответе.
- 🔴 00:45 Тест на weighted-fusion: scores должны отличаться при разных весах. Первоначальный тест сравнивал top-1 IDs — они совпали (один документ доминирует в обоих списках). Переписан на сравнение scores — 63/63 зелёных.
- 🔴 00:50 `.mcp.json` создан в vault пользователя (не в repo — vault снаружи). Содержит абсолютные пути и все RRF-параметры явно. Шаблон: копировать файл + менять `SMART_VAULT_PATH` и `SMART_VAULT_NAME` (опционально имя сервера).
- 🔴 00:55 Обновлены README.md, README_rus.md (большой блок про weighted RRF + предупреждение что k не смещает баланс + секция «.mcp.json как шаблон»), CHANGELOG v2.1.0, package.json=2.1.0.
- 🔴 01:00 Коммит `7ca5d36` «feat(search): weighted hybrid RRF + doc README_rus; v2.1.0» — 7 файлов, +1477/−14. Ветка теперь имеет 8 коммитов. Не запушено.
- 🟡 01:05 Пользователь собирается тестировать MCP-сервер самостоятельно, правки — в следующей сессии. Нужен HANDOFF с чётким TL;DR для быстрого возврата в контекст.

- 🔴 14:00 **Отчёт тестирования `test-reports/TESTING_REPORT_1.md` получен** — 459 строк. Все 9 tools прошли технически. Критических дефектов нет. Главная проблема — UX-разрыв: дефолтный excerpt 500 симв. против `##`-блока 3-6k симв. покрывает 8-16% → агент получает topic-фразу вместо ответа.
- 🔴 14:02 Реальный кейс из отчёта: запрос «как удалить поле в Working Memory со Zod-схемой», правильный ответ «установить в `null`» — поиск нашёл нужный блок (125 строк, sim высокая), но ответ был на 70-м символе из 4500 — excerpt обрезал на intro-таблице.
- 🔴 14:03 Три уровня блоков Smart Connections (`##`, `###`, `#{N}`-фрагменты) ранжируются в одном списке: top-5 часто занят дублями одного раздела (4 из 5 слотов). Нужна дедупликация по `##`-префиксу.
- 🔴 14:05 `threshold=0.5` в `mode=hybrid` **игнорируется** (search-engine.ts:231-255 передаёт 0 в обе ветки, post-filter отсутствует). Агент видит similarity ~0.011 и не понимает шкалу RRF.
- 🔴 14:10 Код — ключевые точки реализации expand-to-section: (a) heading SmartBlock хранится с ведущими `#` — `"#Section#Sub#{1}"` (types.ts:86-103); (b) `extractBlockContent(path, heading)` уже есть в loader (smart-connections-loader.ts:484-491); (c) `rankByVector()` — централизованная сборка hit'ов в search-engine.ts:426-501 — идеальная точка post-processing; (d) `blockByKey: Map<string, SmartBlock>` для parent-lookup возможно надо добавить.
- 🔴 14:12 Алгоритм parent-lookup (итеративное срезание): `lastIndexOf("#")` → cut → strip trailing `#` → if `!includes("#")` return null → else lookup в `blockByKey`. Итерация до первого hit. Для `##A#B#{1}` → пробует `##A#B` → если нет → `##A` → если нет → null (note-level).
- 🔴 14:15 Составлен план v2.2.0 `.claude/plans/1-inherited-panda.md` — 6 коммитов, 2 итерации + backlog. Context + критические файлы + verification. Утверждён пользователем.
- 🔴 14:20 Ответы пользователя на 2 развилки: (1) Skill — **оба**: файл в `.claude/skills/obsidian-knowledge-search/` проекта + симлинк из `~/.claude/skills/`; (2) Релиз — **серия коммитов по итерациям**.
- 🔴 14:25 Пользователь корректирует порядок: **Skill — В САМОМ КОНЦЕ**. Сначала все код-правки, потом skill, потом docs. Новый порядок коммитов: 1) excerpt/cap defaults, 2) expand+dedup, 3) rank_score+fuzzy, 4) smoke, 5) skill+symlink, 6) docs+v2.2.0 bump.
- 🟡 14:27 Риск-флаг: `similarity_threshold` как алиас старого `threshold` — может создать путаницу в tool-schema. Проще оставить `threshold` с расширенной документацией, а для hybrid применить threshold к cosine-компоненте пред-RRF. Решено при реализации — не ломать имена параметров.

- 🔴 15:00 Коммит 1 `7199f15` `feat(ux): raise excerpt/note-content defaults`. excerpt_chars 500→1500 (в Zod + JSON schema + внутренний DEFAULT), MAX_NOTE_CONTENT_CHARS 100k→200k, уточнение descriptions `search_notes` (RRF vs cosine, threshold pre-fusion) и `get_note_content` (cap + full:true). 63/63 smoke passed.
- 🔴 15:30 Коммит 2 `92b38d3` `feat(search): expand-to-section and deduplicate-by-section`. ~500 строк кода. Новые поля SimilarNote: section_content, section_heading, section_lines, expansion{applied,reason,original_heading,truncated_to_max_chars}, sibling_matches. 5 новых параметров в 4 tool-schemas. Algorithm: parent-lookup через итеративное срезание ключа по `lastIndexOf("#")` + strip trailing `#` от `##`-маркеров. Post-processing в отдельной функции postProcessHits, вызывается из всех публичных методов. В searchByQuery — override similarity для hybrid expand decisions (pre-fusion cosine).
- 🔴 15:35 **Bug fix в sectionKeyFor**: первоначальный regex `/^##[^#]+/` не матчил headings начинающиеся с `#` (H1 as first segment). Переделал на split по `/#+/` + take first N segments. Теперь работает и для `#Title#Section#Sub#{1}` и для `##Section#Sub`. Подтверждено на реальном vault: siblings корректно группируются.
- 🔴 15:40 Ad-hoc verification на пользовательском vault: (a) fragment `#{1}` → section_content = 941 chars parent; (b) hybrid с expand_threshold=0.6 → section_len up to 5000 (truncated), reason="similarity >= threshold" и "fragment auto-expand" вперемешку; (c) dedup схлопывает 3-5 siblings в одну группу.
- 🔴 16:00 Коммит 3 `2d8d473` `feat(search): hybrid rank_score normalization and fuzzy heading lookup`. rank_score = similarity / max(similarity) по финальной выдаче, top-1 всегда 1.0. raw_rrf_score = копия similarity для диагностики. findBlockFuzzy в loader: scope = blocksBySource[path] (10-200 entries), normalization lowercase+trim+collapse whitespace. Ambiguous (>1 match) — throws с candidates. search_blocks и get_block_content используют на exact miss, bubble warnings в meta/response.
- 🔴 16:15 Коммит 4 `df34245` `test: smoke coverage +10` → 73/73 passed. Тесты: fragment auto-expand (section_heading без #{N}, content populated), expand never (no expansion/section_content fields), dedup sibling_matches, rank_score [0,1] + top-1=1.0 + mirror raw_rrf_score, fuzzy lookup (case-mangled + trailing spaces → warnings + canonical). Edge case: fuzzy mangling on cyrillic sometimes ambiguous — handled fallback case.
- 🔴 16:40 Коммит 5 `3ad16d7` `feat(skill): obsidian-knowledge-search agent guide` — 355 строк. 10 секций: триггеры, архитектура (3 уровня блоков, shkalы similarity), карта 9 tools → задачи, формат ответа, critical rules (всегда expand для #{N}, не путать scale RRF vs cosine, дедуп уже сделан сервером), research-паттерн, 4 worked examples, антипаттерны, error dictionary, quick reference. После git add skill автоматически подхватился Claude Code (виден в available skills).
- 🔴 17:00 Коммит 6 `[финал]` — docs обновление + package.json 2.1.0→2.2.0. README.md: новая секция про post-processing knobs таблицей, обновлён формат hit'а, note про 70+ smoke, секция про skill + симлинк команда. README_rus.md: новая секция "Что нового в v2.2.0" (6 пунктов). CHANGELOG.md: полный раздел v2.2.0. 
- 🔴 17:10 Skill-симлинк: пользователь выбрал "оба" — файл в проекте + симлинк в ~/.claude/skills/. Команда создания симлинка включена в README. Симлинк создаётся пользователем вручную (мы не создаём его от имени пользователя).
- 🟡 17:15 Backlog для будущих итераций: (1) MCP SDK migration от deprecated Server API; (2) BM25 index для keyword mode на больших vault'ах; (3) INCLUDE_FRAGMENT_BLOCKS=false env как альтернатива expand; (4) canvas structure parsing; (5) resolve_link edge cases (obsidian://, block-refs, alias).

- 🔴 17:30 Пользователь попросил создать отдельный MD-документ с описанием функциональных изменений v2.2.0 для удобства ручного тестирования. Разместил в `test-reports/CHANGES_v2.2.0.md` — рядом с TESTING_REPORT_1.md. Документ ориентирован на практику: что нового → как проверить в Claude Code → ожидаемый сигнал в ответе. Пользователь вернётся с результатом после теста в следующей сессии.

## Дата: 2026-04-23

- 🔴 11:00 Пользователь передал `test-reports/TESTING_REPORT_v2.2.0.md` — полный прогон 9 tools на реальном vault (143 заметки, 11915 блоков, bge-m3:latest, dims=1024). Все 9 tools функционально работают. Подтверждены все v2.2.0-фичи: expand, dedup, rank_score/raw_rrf_score, fuzzy heading lookup, увеличенные дефолты, watcher hot-reload (11913→11915 между вызовами).
- 🔴 11:02 **Проблема 1 (средняя) — блокирующая**: `get_similar_notes` с `granularity: "note"` и `limit: 5` → ответ 160k+ символов → token-limit клиента. Корневая причина: каждый hit тянет полный `blocks: [heading_keys]` (у 152k-заметки ~30k символов только на blocks). `include_excerpt: false` не спасает — excerpt'ы не являются основной долей bloat'а. Решение: новый параметр `include_blocks_list: boolean` (default `false` для get_similar_notes — поле дублируется N раз) + cap `max_blocks_per_hit` (default 30) с флагом `blocks_truncated`.
- 🔴 11:04 **Проблема 2 (низкая)**: `get_note_content` cap `MAX_NOTE_CONTENT_CHARS = 200000` применяется только к `content`, но не к полному JSON. На заметке 152k `content` → JSON ~234k (content+blocks[]+meta). `meta.truncated: false`, но клиент не может прочитать. Решение: тот же механизм — `include_blocks_list` (default `true` для совместимости) + cap на количество элементов blocks[] (например 150) с флагом truncation.
- 🔴 11:05 **Проблема 3 (косметическая)**: `get_block_content` на несуществующем heading возвращает «Block line range unknown for <key>», а README/CHANGES обещают «Block not found: <key>». Решение: переименовать сообщение в коде.
- 🔴 11:08 Не покрыто тестами (gap): path-traversal guard серверный (блокирован permission-system клиента до сервера), `DISABLE_SEMANTIC_SEARCH=1` fallback, `.canvas` whitelist (нет файлов в vault), `expand_to_section: "always"` и `"never"` режимы. Готовы кандидаты на smoke-тесты.
- 🔴 11:10 Производительность в норме: get_stats 3ms, search_notes hybrid 1240ms (первый раз с Ollama), search_notes semantic 170ms, get_similar_notes 42ms, search_blocks 41ms, graph 2ms, note_content 2-5ms, block_content 2-4ms, resolve_link 0-1ms. Нет перформанс-проблем — только response-size.
- 🔴 11:12 Ключевой вывод отчёта: v2.2.0 решил исходную проблему прошлого отчёта («вопрос про удаление поля Working Memory через null») — теперь excerpt 1500 вмещает прямой ответ, expand_to_section для фрагментов выдаёт полный раздел, search_blocks с cosine 0.94 сразу возвращает `section_content` с ответом. Все три фичи работают в ожидаемой связке.
- 🟡 11:15 Релиз-стратегия: v2.2.1 как патч-релиз. Итерация 1 — проблемы 1+2+3 (коммиты fix/errors/fix-response-size). Итерация 2 — smoke-тесты (expand always/never, include_blocks_list), косметика. Итерация 3 — docs + bump.
- 🟡 11:16 Возможная breaking change: если менять default `include_blocks_list` на `false` в `get_similar_notes` — потребители, полагающиеся на `blocks[]`, могут удивиться. Но поле и так редко использовалось (в отчёте пользователь вообще не ссылался на него). Решение: сделать `false` — безопасно.
- 🟡 11:18 `meta.response_bytes` как diagnostic-поле — опциональная полезность: агент может оценить близость к token-limit до попытки чтения. Взвесить ценность vs лишнее поле в meta. Не в первой итерации.
- 🔴 11:25 План v2.2.1 представлен пользователю. 4 коммита в итерациях: (1) fix response-size = include_blocks_list + max_blocks_per_hit + max_blocks; (2) unified error "Block not found"; (3) smoke +8 тестов; (4) docs+bump.
- 🔴 11:28 Пользователь принял ВСЕ дефолты плана: include_blocks_list=false в get_similar_notes, true в get_note_content, та же ветка, skill в коммите docs. Это финальная стадия — готов продукт.
- 🔴 11:30 Стартую реализацию v2.2.1. Порядок: прочитать search-engine.ts/types.ts/index.ts → правки → build → smoke → docs → коммиты.
- 🔴 12:15 v2.2.1 выпущена — 4 коммита `3b7193f` (response-size fix), `3e95429` (Block not found), `c720e75` (smoke 73→86), `cb37c87` (docs+bump). Ветка 18 коммитов, не запушена. Smoke 86/86. Build чистый.
- 🔴 14:00 TESTING_REPORT_v2.2.1.md получен. **Все 9 tools — OK**, все 3 v2.2.1-фикса подтверждены на живом vault (145 заметок, 11987 блоков). Дубль-ответ на Working Memory/Zod-запрос с RRF→cosine конверсией работает. Все breaking defaults поняты и срабатывают.
- 🔴 14:02 **Финальная проблема**: заметка 152k символов с `include_blocks_list: false` отдаётся валидным JSON'ом (truncated:false), но `content` упирается в token-limit клиента. Пользователь просит поднять cap до эквивалента 200k токенов. У пользователя Opus 4.7 с 1M context — модель это переварит; вопрос в клиентском MCP-лимите.
- 🔴 14:04 **Ключевой нюанс**: сервер-cap `MAX_NOTE_CONTENT_CHARS = 200_000` — в **символах**, не в токенах. Клиентский token-limit MCP — отдельное ограничение, сервер на него не влияет. 200k символов ≈ 50k токенов (на русском ≈ 60-70k). Для 200k токенов ответа нужно ~600-800k символов на русском, ~800k-1M на смешанном тексте.
- 🟡 14:06 Варианты ответа: (1) поднять `MAX_NOTE_CONTENT_CHARS` до 800_000 или 1_000_000 (~200-250k токенов русского); (2) полностью удалить cap, полагаясь на `full: true` контроль со стороны агента; (3) объяснить пользователю что клиентский token-limit MCP-сервер не обходит. Мне нужно честно всё это перевести на выбор.
- 🔴 14:20 Пользователь выбрал Вариант A: поднять default `MAX_NOTE_CONTENT_CHARS` до 1_000_000 + env-configurable. Также попросил объяснить что такое `get_note_content_chunk` для chunked-чтения.
- 🔴 14:22 Стартую реализацию: константа 200_000 → 1_000_000, env-переменная `MAX_NOTE_CONTENT_CHARS` через parseNum, обновить docs (README, README_rus, CHANGELOG, skill, CHANGES_v2.2.1).
- 🟡 14:25 Chunked reading варианты: (a) char-based offset/length — простой но режет посреди слов; (b) line-based from_line/to_line — совместим с `lines: [start, end]` из search hits, лучше для markdown; (c) существующий `get_block_content` — это уже chunked через semantic boundaries. Реально новый tool нужен только для заметок без чётких heading'ов или для cross-section чтения.
- 🔴 14:50 v2.2.2 закоммичена (`d21ecf3`). Ветка 19 коммитов. Пользователь подтвердил: v2.2.2 готов как финальный релиз.
- 🔴 15:00 Получен TESTING_REPORT_v2.2.2.md. Vault теперь 147 notes, 12007 blocks. Все 9 tools OK. Подтверждён cap 1M (заметка 421k вернулась `truncated: false`). **§4.1 finding**: в README_rus.md в описании `search_blocks` было ошибочно написано «не возвращает блоки из той же заметки». Фактически код исключает ТОЛЬКО сам `block_key`. Решение: переписать фразу.
- 🔴 15:10 Коммит `1505d9d` — README_rus.md строки 994-999 приведены в соответствие с кодом; там же `excerpt_chars` default 500→1500 (реальный default с v2.2.0). Ветка 20 коммитов.
- 🔴 15:20 Пользователь просит проверить `test-reports/.mcp.json` (копия шаблона). Вопрос: отражены ли все env-переменные, включая `MAX_NOTE_CONTENT_CHARS` из v2.2.2? Также указывает на README_rus.md строки 791-826 как на секцию шаблона — нужна сверка.
- 🔴 15:30 Верно — `MAX_NOTE_CONTENT_CHARS` отсутствовал в шаблоне. Исправлено в `test-reports/.mcp.json` (ФС, .gitignore) и в README_rus строки 791-826 (коммит `005606e`). Добавлена полная справочная таблица по каждой env и явный список опциональных env. Stop-test: startup-лог подтвердил `(from env)`.
- 🔴 15:40 Пользователь просит проверить SKILL.md — нужен ли ещё один проход после всех правок v2.2.x. Читаю и сверяю с актуальным состоянием.
- 🔴 16:00 Найдено две устаревших точки в SKILL.md: (1) §9 error table содержит ретро-сообщение "Block line range unknown" (убрано в v2.2.1); (2) §4 пример JSON использует v2.2.0 totals (141/11796). Обе исправлены в `849cad8`.
- 🔴 16:15 Пользователь поднимает универсальность skill: он использует skill не только для vault 'Develop', значит цифры `total_notes: 147, total_blocks: 12007` и другие vault-specific значения в JSON-примере §4 вводят в заблуждение — агент может подумать что это fixed defaults. Нужна разметка примера: что vault-specific (переменное), что server-default (фиксировано), что per-request (меняется на каждом вызове).
- 🟡 16:18 Классификация полей в meta: vault-specific = `vault_name`, `model_key`, `dims`, `total_notes`, `total_blocks`; server-default = `fusion.{k,semantic_weight,keyword_weight}`, `expansion.{mode,threshold,max_chars}`, `dedup.level`; per-request = `execution_ms`, `applied_count`, `skipped_count`, `groups_collapsed`, весь `results[]`; state = `semantic_available`, `search_mode`, `fallback_from`, `warnings`.
- 🔴 16:30 Коммит `f3208b2` — SKILL.md §4 получил legend-таблицу (4 категории полей) + inline-аннотации на каждом поле JSON-примера. Теперь skill полностью vault-agnostic: агент, открывая пример, сразу видит природу каждого значения и не путает vault-specific цифры с серверными дефолтами.
- 🔴 16:40 Wrapup сессии. Итог сессии: превратили v2.2.0 в v2.2.2 через 3 патча + финализация документации. Ветка `feat/v2-bge-m3-and-hardening`, 26 коммитов (моих), не запушена. Smoke 86/86. Build чистый. Все 9 tools live-протестированы. Все документы синхронизированы под v2.2.2 и vault-agnostic.
- 🔴 16:42 Выпущены в рамках сессии: v2.2.1 (4 коммита), v2.2.2 (1 коммит), плюс 4 post-release доккоммита (search_blocks self-exclusion fix в README_rus, MAX_NOTE_CONTENT_CHARS в .mcp.json шаблоне, skill error-table cleanup, skill §4 legend). Всего 9 коммитов за сессию. Проект готов к повседневной эксплуатации — пользователь так сформулировал в TESTING_REPORT_v2.2.2.md §5.
